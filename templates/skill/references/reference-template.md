# Reference title

Background the skill points at but does not need in every run — the full spec,
the field-by-field contract, the edge cases, the worked example.

Claude loads this file only when the SKILL.md body links to it and the run
actually needs it, so detail here is close to free. Put the long material in
files like this one and keep SKILL.md to the steps.

Link back from SKILL.md by relative path:

    Full contract, including why each rule exists: `references/reference-template.md`
